This is where the logic goes for the text intent. It is called when the user responds to the main intent. It will echo back what the user told the main intent.
[source,groovy]
./src/main/groovy/org/grails/gactions/TextRequestResponseHandler.groovy
----
include::{sourceDir}/src/main/groovy/org/grails/gactions/TextRequestResponseHandler.groovy[indent=0]
----
